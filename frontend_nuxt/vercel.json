{
  "buildCommand": "chmod -R 755 node_modules/.bin && npx prisma generate && npx nuxt build",
  "installCommand": "npm install",
  "framework": "nuxtjs",
  "crons": [
    {
      "path": "/api/cron/deadline-rappels",
      "schedule": "0 7 * * *"
    }
  ]
}
